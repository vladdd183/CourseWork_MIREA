// ============================================================================
// 🎬 SHOWCASE — демо-документ для скриншотов README
// ============================================================================
// Этот файл НЕ является частью шаблона. Он создан для генерации скриншотов,
// показывающих возможности шаблона в README.md
// ============================================================================

#import "../../meta.typ": formatting
#import "../../lib/core.typ": apply-template
#import "../../lib/elements.typ": cite-src, cite-range, tbl, long-tbl, code-block, fig-placeholder
#import "../../lib/diagrams.typ": gost-plot
#import "@preview/cetz-plot:0.1.3": plot

#let meta = (
  doc-type: "coursework",
  university: "МИРЭА — Российский технологический университет",
  institute: "Институт информационных технологий",
  department: "Кафедра инструментального и прикладного программного обеспечения",
  discipline: "Программное обеспечение облачных и распределённых вычислительных систем",
  title: "Разработка микросервисной платформы мониторинга облачной инфраструктуры с использованием Kubernetes",
  variant: 7,
  student: (name: "Иванов Иван Иванович", group: "БСБО-01-21"),
  supervisor: (name: "Петров П.П.", title: "к.т.н., доцент"),
  city: "Москва",
  year: "2026",
  formatting: formatting,
  abbreviations: (
    (abbr: "API", full: [Application Programming Interface — программный интерфейс приложения]),
    (abbr: "БД", full: [База данных]),
    (abbr: "ПО", full: [Программное обеспечение]),
    (abbr: "CI/CD", full: [Continuous Integration / Continuous Delivery — непрерывная интеграция и доставка]),
    (abbr: "gRPC", full: [Google Remote Procedure Call — протокол удалённого вызова процедур]),
    (abbr: "REST", full: [Representational State Transfer — передача репрезентативного состояния]),
  ),
)

#show: apply-template.with(meta: meta)

// ============================================================================
// ВВЕДЕНИЕ
// ============================================================================

#heading(level: 1, numbering: none)[Введение]

*Актуальность.* В условиях массового перехода корпоративных информационных систем в облачную среду возрастает потребность в инструментах мониторинга, способных работать в распределённых кластерах Kubernetes. Существующие решения (Prometheus, Grafana, Zabbix) требуют значительной настройки и не всегда обеспечивают сквозную видимость микросервисной архитектуры #cite-src(1, 2).

*Цель работы* — разработать микросервисную платформу мониторинга облачной инфраструктуры, обеспечивающую сбор, агрегацию и визуализацию метрик в реальном времени.

*Задачи:*
+ Проанализировать существующие решения мониторинга облачной инфраструктуры.
+ Спроектировать архитектуру микросервисной платформы мониторинга.
+ Реализовать основные компоненты системы: коллектор метрик, агрегатор, хранилище временных рядов и веб-интерфейс.
+ Провести нагрузочное тестирование и оценить производительность.

*Объект исследования* — процесс мониторинга облачной инфраструктуры на базе Kubernetes.

*Предмет исследования* — методы и средства разработки микросервисных систем мониторинга.

// ============================================================================
// ГЛАВА 1
// ============================================================================

= Анализ предметной области

== Обзор облачных платформ мониторинга

Современные системы мониторинга облачной инфраструктуры можно разделить на три основные категории: проприетарные SaaS-решения, open-source платформы и гибридные системы #cite-src(3). Каждая категория имеет свои преимущества и ограничения, которые необходимо учитывать при выборе технологического стека.

Согласно исследованиям #cite-range(4, 7), основными критериями выбора системы мониторинга являются: масштабируемость, задержка обработки метрик, поддержка кастомных метрик и стоимость эксплуатации.

== Сравнительный анализ существующих решений

#tbl(
  "Сравнение систем мониторинга облачной инфраструктуры",
  columns: (1.5fr, 1fr, 1fr, 1fr, 1fr),
  zebra: true,
  [*Критерий*], [*Prometheus + Grafana*], [*Datadog*], [*Zabbix*], [*Наша платформа*],
  [Масштабируемость], [Средняя], [Высокая], [Низкая], [Высокая],
  [Задержка метрик], [15–60 с], [< 5 с], [30–120 с], [< 3 с],
  [Кастомные метрики], [Да (PromQL)], [Да], [Ограничено], [Да (gRPC)],
  [Кластеризация], [Federation], [Авто], [Proxy], [Kubernetes-native],
  [Стоимость (50 нод)], [Бесплатно], [\$750/мес], [Бесплатно], [Бесплатно],
  [Open Source], [Да], [Нет], [Да], [Да],
  [Dashboard-as-Code], [JSON/YAML], [Terraform], [Нет], [Typst + YAML],
)

На основе проведённого анализа (таблица выше) определены ключевые преимущества разрабатываемой платформы: нативная интеграция с Kubernetes, минимальная задержка обработки метрик и полная открытость исходного кода.

== Математическая модель агрегации метрик

Для агрегации временных рядов используется экспоненциальное скользящее среднее (EMA), которое позволяет сглаживать шумные метрики:

$ "EMA"_t = alpha dot x_t + (1 - alpha) dot "EMA"_(t-1) $

где $alpha = 2 / (N + 1)$ — коэффициент сглаживания, $N$ — размер окна. Для обнаружения аномалий используется Z-score:

$ z = (x - mu) / sigma, quad "где" mu = 1/n sum_(i=1)^n x_i, quad sigma = sqrt(1/n sum_(i=1)^n (x_i - mu)^2) $

Метрика считается аномальной, если $|z| > 3$ (правило трёх сигм). Для мультимерных метрик применяется расстояние Махаланобиса:

$ D_M (arrow(x)) = sqrt((arrow(x) - arrow(mu))^T Sigma^(-1) (arrow(x) - arrow(mu))) $

== Выводы по главе

В результате анализа установлено, что существующие решения не обеспечивают одновременно низкую задержку, нативную интеграцию с Kubernetes и возможность декларативного описания дашбордов. Это обосновывает необходимость разработки собственной платформы.

// ============================================================================
// ГЛАВА 2
// ============================================================================

= Проектирование системы

== Архитектура микросервисной платформы

Система спроектирована по принципу разделения ответственности (SoC) и состоит из пяти основных микросервисов, каждый из которых выполняет строго одну функцию.

#fig-placeholder("Архитектура микросервисной платформы мониторинга")

== Выбор технологий

#tbl(
  "Технологический стек платформы",
  columns: (1fr, 1.5fr, 2fr),
  zebra: true,
  [*Компонент*], [*Технология*], [*Обоснование*],
  [Язык бэкенда], [Go 1.22], [Высокая производительность, горутины],
  [Транспорт], [gRPC + Protobuf], [Типизация, бинарный формат, стриминг],
  [Хранилище метрик], [TimescaleDB], [Оптимизация для временных рядов],
  [Кэш], [Redis Cluster], [Sub-ms латентность, Pub/Sub],
  [Оркестрация], [Kubernetes 1.29], [Авто-скалирование, self-healing],
  [CI/CD], [GitLab CI + ArgoCD], [GitOps, декларативное развёртывание],
  [Фронтенд], [React + D3.js], [Интерактивные графики, WebSocket],
)

== Выводы по главе

Спроектирована архитектура из пяти микросервисов с чётким разделением ответственности. Выбранный технологический стек обеспечивает горизонтальную масштабируемость и минимальную задержку обработки метрик.

// ============================================================================
// ГЛАВА 3
// ============================================================================

= Реализация и тестирование

== Реализация коллектора метрик

Коллектор метрик реализован на Go с использованием gRPC-интерфейса для приёма метрик от агентов. Ниже представлен ключевой фрагмент обработки входящего потока метрик:

#code-block("Обработчик потока метрик (Go)")[
  ```go
  func (s *CollectorServer) StreamMetrics(
      stream pb.Collector_StreamMetricsServer,
  ) error {
      for {
          metric, err := stream.Recv()
          if err == io.EOF {
              return stream.SendAndClose(&pb.Ack{
                  Count: s.processed.Load(),
              })
          }
          if err != nil {
              return status.Errorf(codes.Internal,
                  "receive error: %v", err)
          }

          // Валидация и агрегация
          if err := s.validate(metric); err != nil {
              s.metrics.InvalidCount.Inc()
              continue
          }

          // EMA-сглаживание
          smoothed := s.ema.Update(metric.Name, metric.Value)

          // Проверка аномалий (Z-score > 3σ)
          if s.detector.IsAnomaly(metric.Name, smoothed) {
              s.alertCh <- Alert{
                  Metric: metric.Name,
                  Value:  smoothed,
                  Time:   time.Now(),
              }
          }

          // Запись в TimescaleDB (батч)
          s.buffer.Add(metric.Name, smoothed, metric.Timestamp)
          s.processed.Add(1)
      }
  }
  ```
]

== Результаты нагрузочного тестирования

Нагрузочное тестирование проведено с использованием `k6` на кластере из 5 узлов (16 vCPU, 32 GB RAM каждый). Результаты представлены в таблице ниже.

#long-tbl(
  "Результаты нагрузочного тестирования",
  columns: (auto, 1fr, auto, auto, auto),
  zebra: true,
  header: ([*\#*], [*Сценарий*], [*RPS*], [*P99 (мс)*], [*Ошибки*]),
  [1], [Запись одиночных метрик], [45 000], [12], [0.00%],
  [2], [Пакетная запись (batch 100)], [180 000], [28], [0.01%],
  [3], [Стриминг (gRPC)], [320 000], [8], [0.00%],
  [4], [Чтение за последний час], [12 000], [45], [0.00%],
  [5], [Агрегация (5-мин окно)], [8 500], [67], [0.02%],
  [6], [Запрос дашборда (10 панелей)], [3 200], [120], [0.00%],
  [7], [Обнаружение аномалий (real-time)], [280 000], [15], [0.01%],
  [8], [Смешанная нагрузка (80/20)], [95 000], [35], [0.01%],
)

== Анализ производительности

На графике ниже представлена зависимость задержки (P99) от количества запросов в секунду:

#figure(
  gost-plot(
    size: (12, 7),
    x-label: [RPS (тыс.)],
    y-label: [P99 задержка, мс],
    x-tick-step: 50,
    y-tick-step: 50,
    x-grid: "major",
    y-grid: "major",
    legend: "inner-north-west",
    {
      // gRPC стриминг
      plot.add(
        domain: (0, 350),
        x => 5 + 0.00005 * calc.pow(x, 1.8),
        label: [gRPC стриминг],
        style: (stroke: (paint: rgb("#2563EB"), thickness: 2pt)),
      )
      // REST API
      plot.add(
        domain: (0, 350),
        x => 15 + 0.001 * calc.pow(x, 1.6),
        label: [REST API],
        style: (stroke: (paint: rgb("#DC2626"), thickness: 2pt)),
      )
      // Prometheus (для сравнения)
      plot.add(
        domain: (0, 200),
        x => 30 + 0.005 * calc.pow(x, 1.5),
        label: [Prometheus],
        style: (stroke: (paint: rgb("#6B7280"), thickness: 1.5pt, dash: "dashed")),
      )
    },
  ),
  caption: "Зависимость P99-задержки от нагрузки",
  supplement: "Рисунок",
)

Разработанная платформа демонстрирует P99-задержку менее 35 мс при нагрузке до 100 000 RPS, что в 3 раза ниже аналогичного показателя Prometheus #cite-src(8).

== Выводы по главе

Реализованная платформа обеспечивает обработку до 320 000 метрик в секунду через gRPC-стриминг с P99-задержкой менее 8 мс. Нагрузочное тестирование подтвердило линейную масштабируемость системы при добавлении узлов кластера.
